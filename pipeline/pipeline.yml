name: food_security_pipeline
schedule: "0 6 1 1,4,7,10 *"
start_date: "2024-01-01"
catchup: false

default_connections:
    duckdb: "duckdb_default"

default:
    type: duckdb.sql

variables:
    start_year:
        type: integer
        default: 2020
    end_year:
        type: integer
        default: 2024
